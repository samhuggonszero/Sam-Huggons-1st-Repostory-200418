SELECT * FROM usage.scopus.dim_document 
where document_issn='00123692'
LIMIT 100;

document_pub_year, 


'00379646'

select * from spectrum_docs.documents limit 100

document_doi, document_eid, 


is_author, 




select count(distinct(profile_id)),
count(*)

from spectrum_docs.documents a
left join scopus.dim_document b
on a.doi=b.document_doi
where document_issn='00123692'
and is_author=1

1689

select count(distinct(profile_id)),
count(*)

from spectrum_docs.documents a
left join scopus.dim_document b
on a.doi=b.document_doi
where document_issn='00123692'
and is_author=1
and document_pub_year>=2013

doi, 